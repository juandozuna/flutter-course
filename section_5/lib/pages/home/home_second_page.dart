import 'package:flutter/material.dart';

class HomeSecondPage extends StatelessWidget {
  const HomeSecondPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.red,
      child: Center(child: Text('Home Second Page')),
    );
  }
}
