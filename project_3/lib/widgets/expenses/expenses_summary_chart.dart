import 'package:flutter/material.dart';

class ExpensesSummaryChart extends StatelessWidget {
  const ExpensesSummaryChart({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
