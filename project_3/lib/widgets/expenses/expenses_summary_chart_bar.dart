import 'package:flutter/material.dart';

class ExpensesSummaryChartBar extends StatelessWidget {
  const ExpensesSummaryChartBar({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
